# ATrader

Execute the project using below command line statements

1. mvn clean verify
2. bash mvn allure:serve - This will generate allure report.

Note : Chrome driver and place in resource folder.
